{#{% trans_default_domain 'FOSUserBundle' %}#}

{#{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}#}
    {#{{ form_widget(form) }}#}
    {#<div>#}
        {#<input type="submit" value="{{ 'registration.submit'|trans }}" />#}
    {#</div>#}
{#{{ form_end(form) }}#}



{% form_theme form 'form/bootstrap-form-field.html.twig' %}

<form  action="{{ path('fos_user_registration_register') }}" method="POST" class="fos_user_registration_register form-horizontal">

    {{ form_widget(form) }}

    {#{{ form_row(form.username) }}#}
    {#{{ form_row(form.email) }}#}
    {#{{ form_row(form.plainPassword) }}#}

    <div class="form-group">
        <div class="col-xs-offset-3 col-xs-10 login-btn">
            <input class="btn btn-lg btn-success" type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
        </div>
    </div>

</form>

<div class="row">
    <div class="col-md-12">
        <h2 class="big_header">Have you got an account? <a href="{{ path('fos_user_security_login') }}">Log in</a> </h2>
    </div>
</div>